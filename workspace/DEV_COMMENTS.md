# Developer Comments Ledger
_Auto-generated by blast-through. Append; never rewrite._

## 2026-06-04 — Full Audit Pass

### High-Signal Comments

| File | Line | Tag | Content |
|---|---|---|---|
| `diabetic/main.py` | 83 | inline note | `#limit days, this was hardcoded, fix later` — `run_retention_cleanup(days=180)` is hardcoded in admin CLI; was supposed to read from config |
| `diabetic/ingestion/nightscout.py` | 220 | inline note | `# Just return empty if this fallback succeeds for simplicity right now` — treatment fallback discards the successful response; silently returns [] |

### Minor (print-in-production)

| File | Line | Content |
|---|---|---|
| `diabetic/config.py` | 156 | `print("Configuration validated: System ready.")` — should use logger |
| `diabetic/telegram_bot/twa_api.py` | 118 | `print("🚀 Bio-Quant TWA Bridge Interface Loaded.")` — module-level print |
| `diabetic/dsp/context_classifier.py` | 102-152 | Multiple `print()` statements inside `if __name__ == "__main__"` — acceptable (test harness) |

## 2026-07-23 — Deep Blast-Through

### High-Signal Comments

| File | Line | Tag | Content |
|---|---:|---|---|
| `diabetic/ingestion/nightscout.py` | 124-125 | dangerous assumption | Comment says values below 30 are almost certainly mmol, but code uses `< 40`; an unlabelled 39 mg/dL severe low is treated as 39 mmol/L. |
| `diabetic/ingestion/mongo.py` | 369-372 | duplicated dangerous assumption | Mongo mapping repeats the same unit guess, creating REST/Mongo parity around the same unsafe behavior. |
| `diabetic/ingestion/weather.py` | 87-89 | incomplete production path | Real five-day forecast is absent and always returns mock data, including outside mock mode. |
| `diabetic/ingestion/cardiac.py` | 99-103 | false provenance | Mock readings omit `source`, so `CardiacReading` labels them as `ble`. |
| `diabetic/ml_engine/train.py` | 134-170 | unsafe promotion | Writes directly to the deployed path before guards, then deletes it when validation fails. |
| `diabetic/coordinator.py` | 528 | config drift | Scheduled retention hardcodes 180 instead of `config.RETENTION_DAYS`. |
| `diabetic/utils/health.py` | 24-36 | misleading readiness | Documentation admits no live probe, but Mongo collection handles are reported as `"ok"`. |
| `run_graphify.py` | 44-93 | destructive degradation | Missing semantic credentials do not stop report generation; AST-only output can overwrite the existing semantic report. |

## 2026-07-24 - Deep Blast-Through

### High-Signal Comments

| File | Line | Tag | Content |
|---|---:|---|---|
| `diabetic/auth/authorization.py` | 39-42 | ownership leak | Any registry row authorizes access to the singleton patient pipeline, despite the documented single-patient boundary. |
| `diabetic/telegram_bot/twa_api.py` | 179 | cross-user mutation | Calibration ignores the authenticated identity and always writes `config.USER_ID`. |
| `diabetic/telegram_bot/decision_matrix.py` | 71-91 | unsafe provenance | Predicted HR substitutes for real cardiac telemetry and can suppress a predicted-hypoglycemia warning as exercise. |
| `diabetic/telegram_bot/decision_matrix.py` | 157-161 | unsafe adaptation | User false-alarm feedback can suppress a current critical-hyper alert above the configured threshold. |
| `diabetic/ingestion/nightscout.py` | 206-207 | degraded-state collapse | Provider failure returns the same empty shape as a successful no-treatment result. |
| `diabetic/utils/health.py` | 117-121 | false readiness | Ready ignores last-reading freshness, loaded inference state, and active inference. |
| `diabetic/utils/temporal.py` | 53 | experimental production feature | Source labels the multiplier pseudoscience while it remains wired into training, inference, and twin forecasts. |

### 2026-07-24 Resolution

- Ownership leak and cross-user mutation: closed by patient/caregiver-only
  authorization and explicit singleton calibration coverage.
- Unsafe cardiac provenance and critical feedback dampening: closed with direct
  decision-matrix tests.
- Treatment degraded-state collapse: closed with `TreatmentFetchResult`, REST
  fallback, and bounded last-known-good retention.
- False readiness: closed at code level with separate core/neural readiness.
- Experimental temporal feature: neutralized at `1.0`; model width preserved.

## 2026-07-25 - Historical-Data And Hygiene Audit

### High-Signal Comments

| File | Line | Tag | Content |
|---|---:|---|---|
| `scripts/analysis/neural_refresh_cycle.py` | 42-51 | false success | Concatenates incompatible CSV schemas, calls async training without `await`, then prints success. |
| `diabetic/ingestion/offline/sim_reader.py` | 7-22 | incomplete contract | Claims JSON/CSV replay but always uses `json.load`; CSV and JSONL both fail. |
| `diabetic/ml_engine/train.py` | 202-226 | hidden legacy default | CSV training silently uses PDF-derived `mar23-apr07.csv` with synthetic cardiac unless called internally with another path. |
| `scripts/ops/migrate_nightscout.py` | 98-118 | integrity gap | Restore reads manifest counts but never validates the recorded source hashes. |
| `scripts/tools/extract_historical.py` | 11-29 | stale path | Looks under removed `data/test/ottai_data`; current PDFs are under `storage/raw/test/ottai_data`. |
| `storage/AUDIT.md` | 10-13 | stale trust claim | Labels all processed files ground truth despite mixed PDF extraction and experimental archive generations. |
