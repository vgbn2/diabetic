from datetime import datetime

# =============================================================================
# 🌍 [TEMPORAL CONTEXT INITIALIZATION]
# =Focus: Regional Holiday Discovery and Festival Registration
# =============================================================================
class TemporalEngine:
    """
    Compatibility surface for the retired temporal-resistance feature.

    Calendar labels are not validated metabolic evidence. The static model
    feature remains present for artifact-shape compatibility and is always
    neutral.
    """

    def get_multiplier(self, dt: datetime | None = None) -> float:
        return 1.0

# Singleton instance
temporal_engine = TemporalEngine()
