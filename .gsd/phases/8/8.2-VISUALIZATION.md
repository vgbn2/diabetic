---
phase: 8
plan: 2
wave: 1
depends_on: ["8.1"]
files_modified:
  - "diabetic/ui/visualizer.py"
  - "diabetic/telegram_bot/handlers.py"
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Telegram bot can push a visual chart image of the 4-hour prediction"
    - "Visualizer handles multi-trace plotting (Actual vs. Twin)"
  artifacts:
    - "diabetic/ui/visualizer.py creates PNG/ASCII charts"
---

# Plan 8.2: Forecast Visualization (Charts)

<objective>
Provide visual feedback to the user by rendering the Digital Twin's 4-hour forecast as a chart.

Purpose: Visual confirmation of meal impact and prediction accuracy.
Output: `visualizer.py` and Telegram chart integration.
</objective>

<tasks>

<task type="auto">
  <name>Implement Visualizer Utility</name>
  <files>diabetic/ui/visualizer.py</files>
  <action>
    Create a `MetabolicVisualizer` class:
    - `plot_forecast(actual_history, predicted_array)`: Renders a PNG chart using `matplotlib`.
    - Plot `Actual` (Solid Blue) and `Twin Prediction` (Dashed Red).
    - Annotate the "Predicted Peak" and $T_{max}$.
  </action>
  <verify>Run standalone test to generate a sample `forecast.png`.</verify>
  <done>Visualization utility is capable of plotting metabolic trends.</done>
</task>

<task type="auto">
  <name>Integrate Charting with Telegram</name>
  <files>diabetic/telegram_bot/handlers.py</files>
  <action>
    - Add `/meal` command handler.
    - On `/meal`, trigger `DigitalTwin.get_combined_forecast`.
    - Use `MetabolicVisualizer` to generate a chart.
    - Send the chart as a photo response to the user via Telegram.
  </action>
  <verify>Manually test `/meal` in Telegram if token exists, or simulate feedback in logic tests.</verify>
  <done>User receives visual curve immediately after logging a meal.</done>
</task>

</tasks>

<verification>
- [ ] `visualizer.py` produces a valid image file
- [ ] Chart scale matches physiological ranges (0-25 mmol/L)
</verification>

<success_criteria>
- [ ] User can see the 4-hour impact visually
- [ ] Predicted trace aligns with current trajectory at $t=0$
</success_criteria>
