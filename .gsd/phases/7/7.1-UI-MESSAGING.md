---
phase: 7
plan: 1
wave: 1
depends_on: []
files_modified:
  - "diabetic/ui/cli_hud.py"
  - "diabetic/coordinator.py"
autonomous: true
user_setup: []

must_haves:
  truths:
    - "HUD displays correct units (mmol/L) for velocity and acceleration"
    - "Telegram interactive buttons trigger audit logging and UI updates"
  artifacts:
    - "diabetic/ui/cli_hud.py labels fixed"
    - "diabetic/coordinator.py starts TelegramApp background task"
---

# Plan 7.1: UI & Messaging Fixes

<objective>
Fix the unit label mismatch in the CLI HUD and integrate the Telegram bot callback polling loop to enable interactive alert confirmations.

Purpose: Consistency and interactive feedback are critical for a safety-first system.
Output: Fixed HUD and functioning Telegram feedback loop.
</objective>

<context>
Load for context:
- diabetic/ui/cli_hud.py
- diabetic/coordinator.py
- diabetic/telegram_bot/handlers.py
</context>

<tasks>

<task type="auto">
  <name>Fix HUD Unit Labels</name>
  <files>diabetic/ui/cli_hud.py</files>
  <action>
    Update lines 53-54 to use dynamic units based on `config.PREFER_MMOL`.
    If PREFER_MMOL is True, use `mmol/L/min` and `mmol/L/min²`.
    AVOID: Hardcoding mg/dL because the values themselves are already in mmol/L by default.
  </action>
  <verify>Run `python -m diabetic.main simulation` and check labels in the metrics table.</verify>
  <done>Labels show correct units reflecting the preference.</done>
</task>

<task type="auto">
  <name>Integrate Telegram Callback Loop</name>
  <files>diabetic/coordinator.py</files>
  <action>
    Modify `start_live_mode` to start the `TelegramApp` polling loop in a background task.
    Since `run_polling` can be disruptive, we'll use a `TelegramApp` instance and start its application within the coordinator script if needed.
    AVOID: Blocking the main coordinator loop with the Telegram bot.
  </action>
  <verify>Trigger an alert in simulation and verify that clicking the "Confirmed" button in Telegram updates the message.</verify>
  <done>Telegram feedback loop is active during live/simulation mode.</done>
</task>

</tasks>

<verification>
After all tasks, verify:
- [ ] HUD displays correct units
- [ ] Telegram buttons respond and log feedback
</verification>

<success_criteria>
- [ ] All tasks verified
- [ ] Must-haves confirmed
</success_criteria>
