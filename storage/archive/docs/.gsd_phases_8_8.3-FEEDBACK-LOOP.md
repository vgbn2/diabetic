---
phase: 8
plan: 3
wave: 2
depends_on: ["8.1", "8.2"]
files_modified:
  - "diabetic/ml_engine/twin.py"
  - "diabetic/utils/audit_logger.py"
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Model calculates Correlation/Error Factor after 4 hours of actual data"
    - "System auto-adjusts Carb Sensitivity Factor (CSF) to minimize drift"
  artifacts:
    - "Feedback loop logic implemented in Twin engine"
---

# Plan 8.3: Feedback Correction Loop (Tuning)

<objective>
Close the loop between prediction and reality by comparing the Digital Twin's forecast with actual CGM data, adjusting sensitivity factors for better future accuracy.

Purpose: Model refinement and personalized metabolic modeling.
Output: Dynamic CSF tuning and correlation reporting.
</objective>

<tasks>

<task type="auto">
  <name>Implement Deviation Analysis</name>
  <files>diabetic/ml_engine/twin.py</files>
  <action>
    - Add `analyze_deviation(actual_peak, predicted_peak)` method.
    - Calculate `CorrectionFactor = actual_peak / predicted_peak`.
    - Bound correction to $\pm 20\%$ per iteration to prevent noise instability.
  </action>
  <verify>Unit tests show CSF increasing after an under-predicted meal.</verify>
  <done>Engine can quantify its own prediction error.</done>
</task>

<task type="auto">
  <name>Persistent Self-Tuning</name>
  <files>diabetic/utils/audit_logger.py</files>
  <action>
    - Store the latest tuned `CARB_SENSITIVITY_FACTOR` in a local persistent storage (JSON or Mongo).
    - `Coordinator` loads this factor into `DigitalTwin` on startup.
  </action>
  <verify>Restart system and confirm it remembers the modified sensitivity factor.</verify>
  <done>The twin becomes more accurate over multiple meal cycles.</done>
</task>

</tasks>

<verification>
- [ ] CSF updates after 4-hour window closure
- [ ] Logs show "Model Tuning Applied" with correlation metrics
</verification>

<success_criteria>
- [ ] Correlation between twin and actual improves over 5+ meal cycles
- [ ] System handles sequencing uncertainty by averaging across common GI profiles
</success_criteria>
