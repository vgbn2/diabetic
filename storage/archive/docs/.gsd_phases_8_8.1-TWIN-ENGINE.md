---
phase: 8
plan: 1
wave: 1
depends_on: []
files_modified:
  - "diabetic/ml_engine/twin.py"
  - "diabetic/medical_constants.py"
autonomous: true
user_setup: []

must_haves:
  truths:
    - "DigitalTwin correctly differentiates absorption rates for Fast vs Slow carbs"
    - "Forward simulation generates a 4-hour (48-point) prediction array"
  artifacts:
    - "diabetic/ml_engine/twin.py exists with metabolic equations"
---

# Plan 8.1: Carb Absorption Model & Twin Engine

<objective>
Implement the core mathematical engine for the Digital Twin, modeling how different food types impact glucose over 4 hours.

Purpose: Move from reactive to proactive metabolic management.
Output: `twin.py` engine and updated constants.
</objective>

<context>
- diabetic/medical_constants.py
- diabetic/registry.py
</context>

<tasks>

<task type="auto">
  <name>Define Absorption Constants</name>
  <files>diabetic/medical_constants.py</files>
  <action>
    Add constants for:
    - `CARB_ABS_LIQUID_TAU = 15.0` (Fast absorption peak in mins)
    - `CARB_ABS_STARCH_TAU = 60.0` (Slow absorption peak in mins)
    - `CARB_SENSITIVITY_DEFAULT = 3.0` (Typical 1g carbs = 3 mg/dL or equivalent mmol/L)
  </action>
  <verify>Ensure constants are accessible via `medical_constants.CARB_ABS_LIQUID_TAU`.</verify>
  <done>Constants defined for both fast and slow GI modeling.</done>
</task>

<task type="auto">
  <name>Implement DigitalTwin Engine</name>
  <files>diabetic/ml_engine/twin.py</files>
  <action>
    Create `DigitalTwin` class:
    - `simulate_meal(carbs_g, intensity_type)`: Returns a 48-point array of metabolic impact.
    - Use the equation: $f(t) = (t/tau) * e^{-t/tau}$ (normalized to area = carbs_g * sensitivity).
    - Liquid/Sugar -> Fast ($\tau=15$). Starch/Rice -> Slow ($\tau=60$).
    - `get_combined_forecast(history, meal_event)`: Merges current trajectory with simulated meal impact.
  </action>
  <verify>Run a scratch script to check that `simulate_meal` produces a curved peak.</verify>
  <done>Twin engine can project individual meal impacts based on glycemic index.</done>
</task>

</tasks>

<verification>
- [ ] `twin.py` simulates a 4-hour window
- [ ] Liquid meals peak significantly faster than starch meals
</verification>

<success_criteria>
- [ ] Mathematical model correctly distinguishes GI types
- [ ] Simulation data points are ready for charting
</success_criteria>
