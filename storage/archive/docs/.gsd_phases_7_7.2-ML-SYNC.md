---
phase: 7
plan: 2
wave: 1
depends_on: []
files_modified:
  - "diabetic/ml_engine/predictor.py"
autonomous: true
user_setup: []

must_haves:
  truths:
    - "GlucoseForecaster supports generic `predict(history, horizon)` method"
    - "Predictions include a calculated confidence score (0.0 - 1.0)"
    - "Feature extraction provides all 10 features required by verification scripts"
  artifacts:
    - "diabetic/ml_engine/predictor.py methods synchronized with tests"
---

# Plan 7.2: ML Engine Synchronization

<objective>
Synchronize the ML engine implementation with its verification scripts by adding multi-horizon support, confidence intervals, and the full 10-feature metabolic vector.

Purpose: Enable automated verification and future-proof the prediction logic.
Output: Updated `GlucoseForecaster` passing `verify_prediction.py`.
</objective>

<context>
Load for context:
- diabetic/ml_engine/predictor.py
- scripts/verify_prediction.py
- diabetic/dsp/metabolic_math.py
</context>

<tasks>

<task type="auto">
  <name>Expand Feature Extraction</name>
  <files>diabetic/ml_engine/predictor.py</files>
  <action>
    Update `_prepare_features` to extract 10 features:
    `[val_mmol, v, a, momentum, lbgi, hbgi, t_sin, t_cos, atr, oscillation]`.
    - Use `MetabolicMath.calculate_risk_indices` for LBGI/HBGI.
    - Derived `momentum` as `v * val`.
    - Derive `oscillation` as a simple EMA residual or delta-from-mean.
    - Derive `t_sin/t_cos` from `datetime.now()` for circadian awareness.
  </action>
  <verify>Check `features.shape[1] == 10` in a test script.</verify>
  <done>10-feature vector is correctly populated.</done>
</task>

<task type="auto">
  <name>Implement Multi-Horizon Predict & Confidence</name>
  <files>diabetic/ml_engine/predictor.py</files>
  <action>
    - Add `predict(history, horizon_mins)` method.
    - Implement a confidence decay function: `1.0 - (horizon / 120.0)` capped at 0.1.
    - Update kinematic logic to use `0.5 * A * t^2` term if acceleration is reliable.
    - Return `(prediction, confidence)` tuple.
  </action>
  <verify>Run `python scripts/verify_prediction.py` and confirm all tests pass.</verify>
  <done>Predictor matches the interface expected by verification scripts.</done>
</task>

</tasks>

<verification>
After all tasks, verify:
- [ ] `predict()` returns tuple of (float, float)
- [ ] `verify_prediction.py` passes completely
</verification>

<success_criteria>
- [ ] ML Engine is fully synchronized with test suite
- [ ] Accuracy metrics include confidence thresholds
</success_criteria>
